const TestPage = () => {
  return (
    <div style={{ background: 'white', minHeight: '100vh', padding: '50px' }}>
      <h1>Test Page - Should Load Fast</h1>
      <p>If this loads slowly, the problem is not in the components.</p>
    </div>
  );
};

export default TestPage;
